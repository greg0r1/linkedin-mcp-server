# =============================================================================
# LinkedIn MCP Server - Environment Configuration
# =============================================================================
#
# Instructions:
# 1. Copy this file to .env: cp .env.example .env
# 2. Fill in your LinkedIn app credentials below
# 3. NEVER commit the .env file to git (it's in .gitignore)
#
# =============================================================================

# -----------------------------------------------------------------------------
# LinkedIn OAuth Credentials
# -----------------------------------------------------------------------------
# Get these from: https://www.linkedin.com/developers/apps
#
# 1. Go to LinkedIn Developer Portal
# 2. Click on your app (or create a new one)
# 3. In the "Auth" tab:
#    - LINKEDIN_CLIENT_ID: Copy the "Client ID"
#    - LINKEDIN_CLIENT_SECRET: Click the eye icon to reveal and copy
#
LINKEDIN_CLIENT_ID=your_client_id_here
LINKEDIN_CLIENT_SECRET=your_client_secret_here

# -----------------------------------------------------------------------------
# OAuth Redirect URI
# -----------------------------------------------------------------------------
# This MUST match exactly what you configured in LinkedIn Developer Portal
# Default: http://localhost:3000/auth/callback
#
# To change the port, update both here and in LinkedIn Developer Portal
#
LINKEDIN_REDIRECT_URI=http://localhost:3000/auth/callback

# -----------------------------------------------------------------------------
# Company Page ID (Optional)
# -----------------------------------------------------------------------------
# Only needed if you want to post on behalf of a company page
#
# To find your Company ID:
# - Method 1: Run `npm run get-company-id` after initial authentication
# - Method 2: Go to your company page and inspect the page source for the ID
#
# Note: You must be an administrator of the company page
#
LINKEDIN_COMPANY_ID=

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
# Port for the OAuth callback server (default: 3000)
# Change this if port 3000 is already in use
#
PORT=3000

# Environment mode (development or production)
NODE_ENV=development

# -----------------------------------------------------------------------------
# Token Storage
# -----------------------------------------------------------------------------
# Path where OAuth tokens will be stored (created automatically)
#
# Default: Uses your home directory (~/.linkedin-mcp-tokens.json)
# - macOS/Linux: /Users/username/.linkedin-mcp-tokens.json
# - Windows: C:\Users\username\.linkedin-mcp-tokens.json
#
# You can override this by uncommenting and setting a custom path:
# TOKEN_STORAGE_PATH=/custom/path/to/tokens.json
